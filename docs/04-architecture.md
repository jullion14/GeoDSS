# Repository structure

> Part of the GeoDSS reference docs. See [`README.md`](../README.md) for the index.

```
GeoDSS/                        (root, single Git repo)
├── .gitignore                 (root-level, covers backend + frontend)
├── README.md
├── docs/                      (proposal, Gantt chart, reports)
├── data/                      (source datasets, checked in)
│   ├── MasterPlan2019PlanningAreaBoundaryNoSea.geojson
│   ├── respopagesextod2025e.xlsx
│   ├── GP_Locations.geojson
│   ├── Polyclinics.geojson
│   ├── LTAMRTStationExitGEOJSON.geojson
│   └── bus_stops.geojson       (generated by fetch_bus_data.py)
├── import_data.py             (SINGLE setup script — schema + all imports)
├── fetch_bus_data.py          (LTA DataMall -> bus_stops.geojson)
├── bus_metric_validation.sql  (construct-validity queries, kept for report)
│
├── backend/                   (ASP.NET Core Web API — solution root)
│   ├── GeoDSS.slnx            (.slnx, not .sln — newer XML format)
│   ├── GeoDSS.Api.csproj
│   ├── GeoDSS.Api.http
│   ├── Program.cs             (DI, CORS "FrontendDev", GeoJSON converters)
│   ├── appsettings.json
│   ├── Properties/launchSettings.json   (HTTP :5170 / HTTPS :7269)
│   ├── Controllers/
│   │   ├── PlanningAreasController.cs   GET /, /geojson
│   │   ├── HealthcareController.cs      GET /geojson?type=GP|Polyclinic
│   │   ├── TransitController.cs         GET /geojson
│   │   ├── BusStopsController.cs        GET /geojson?minServices=&planningAreaId=
│   │   ├── AnalysisController.cs        GET /area/{id}, /areas
│   │   ├── PriorityController.cs        priority config + scores
│   │   ├── SensitivityController.cs     GET/POST /api/analysis/sensitivity
│   │   ├── DatasetsController.cs        (NOT YET BUILT — upload)
│   │   └── ExplainController.cs         (NOT YET BUILT — AI)
│   ├── Services/
│   │   ├── SpatialAnalysisService.cs    raw SQL + PostGIS; GetAllAsync,
│   │   │                                GetForAreaAsync
│   │   ├── PriorityScoringService.cs    weighted linear combination
│   │   ├── SensitivityService.cs        ISensitivityService; Monte Carlo,
│   │   │                                tornado, sweep
│   │   ├── ScoringCore.cs               shared normalisation/rank/score maths
│   │   ├── MetricCatalog.cs             metric definitions + default weights
│   │   └── AIExplanationService.cs      (NOT YET BUILT — Gemini)
│   ├── Models/
│   │   ├── PlanningArea.cs
│   │   ├── Population.cs
│   │   ├── HealthcareFacility.cs
│   │   ├── TransitExit.cs
│   │   ├── BusStop.cs
│   │   ├── AccessibilityMetrics.cs      keyless DTO for FromSqlRaw
│   │   └── (priority + sensitivity request/response DTOs)
│   └── Data/
│       └── GeoDssDbContext.cs           snake_case column mapping
│
└── frontend/                  (React + TypeScript, Vite)
    ├── .env                   (NOT committed — VITE_API_URL)
    ├── .env.example
    ├── vite.config.ts, tsconfig*.json, eslint.config.js
    └── src/
        ├── main.tsx
        ├── App.tsx            two views (map | analysis) over shared state;
        │                      map stays mounted across switches
        ├── index.css          theme CSS vars; #root full-bleed
        ├── types/analysis.ts  AccessibilityMetrics interface
        ├── hooks/
        │   ├── useMapLayers.ts      LayerKey, LAYER_META, layer fetch,
        │   │                        visibility, counts
        │   ├── usePriorityScores.ts weights, scores, scoresById
        │   └── useSensitivity.ts    stabilityById, tornadoById
        ├── services/
        │   ├── api.ts               axios instance, baseURL = VITE_API_URL
        │   ├── priorityApi.ts       AreaPriorityScore, MetricDescriptor,
        │   │                        WeightMap
        │   └── sensitivityApi.ts    RankStability, AreaTornado, WeightSweep
        └── components/
            ├── MapView.tsx          presentational only — props in, map out
            ├── LayerPanel.tsx       layers, basemap switcher, shading toggle
            ├── AreaSelector.tsx     selected-area metrics + score breakdown
            ├── RankingStrip.tsx     one-line ranking summary over the map
            ├── AnalysisView.tsx     weights, sensitivity, full table
            ├── ViewTabs.tsx         map | analysis switch
            ├── TornadoChart.tsx     per-area sensitivity chart
            ├── InfoTip.tsx          InfoTip + HELP copy
            ├── basemaps.ts          4 themed basemaps
            └── panelStyles.ts       floatingCard, railButton, chevronStyle,
                                     sectionStyle, surface, colourFor,
                                     scoreColour, areaStroke,
                                     SCORE_RAMP_STOPS, PanelTheme

--- Added for point queries ---------------------------------------
backend/Models/PointAccessibility.cs        point query DTO (keyless entity;
                                            needs HasNoKey().ToView(null) in
                                            GeoDssDbContext, same as
                                            AccessibilityMetrics)
backend/Services/SpatialAnalysisService.cs  + PointSql, GetForPointAsync
backend/Controllers/AnalysisController.cs   + GET /api/analysis/point

frontend/src/services/pointApi.ts           point query client, ProbePoint type
frontend/src/hooks/useProbePoints.ts        probe point list, active point,
                                            within-area spread comparison
frontend/src/components/ProbePanel.tsx      bottom-left measured points panel

--- Search, geolocation, layer theming ----------------------------
SearchController.cs — GET /api/search?q=&types=&perType=
  Four independent indexed queries (GP, polyclinic, MRT exit, bus stop),
  each capped at perType so ~5,000 bus stops cannot crowd out 26
  polyclinics. Returns a flat SearchHit list with a type discriminator.
  Bus matches description, road name, and bus_stop_code by prefix.

useLayerColours.ts — single source of truth for layer colour. Overrides in
  localStorage, merged over LAYER_META defaults. Map markers, panel
  swatches, probe ticks and measurement legs all read from here, so a
  measurement leg always matches the feature it points at. Selection
  highlight and probe teal are deliberately NOT layer colours.

useGeolocation.ts — watchPosition, requested on demand only. Prompting on
  load trains users to deny before they know what it is for, and denial is
  sticky.

MapView helper components (all return null, all direct children of
  MapContainer): InvalidateOnResize, ZoomWatch, DragWatch,
  ProbeClickCapture, PanToActiveProbe, FlyToTarget.

types/map.ts — FlyTarget, shared by search results and the locate button.
  The nonce field exists so flying to the same coordinates twice still
  fires the effect.
```
